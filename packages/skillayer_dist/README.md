# Skillayer

`skillayer` is a lightweight PyPI alias for the Skillayer local coding-agent telemetry CLI.

Install it with:

```bash
pip install skillayer
skillayer connect
```

The implementation is provided by the `skilgen` package so existing `skilgen` users keep their current install path while Skillayer onboarding can use the product command directly.
