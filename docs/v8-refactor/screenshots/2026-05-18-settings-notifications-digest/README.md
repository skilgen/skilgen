# Settings notifications digest screenshots (2026-05-18)

Add desktop + mobile screenshots for the migrated v8 Settings notifications screen:

- `notifications-desktop.png` (desktop-width viewport, e.g. 1440×900)
- `notifications-mobile.png` (mobile-width viewport, e.g. 390×844)

Optional (recommended) additional proof:

- `notifications-preview-desktop.png` (preview dialog open)
- `notifications-preview-mobile.png` (preview dialog open)

Target route:

- `/settings/notifications`

